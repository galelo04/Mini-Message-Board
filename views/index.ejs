<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
  </head>
  <body>
    <ul>
      <% messages.forEach((message)=>{ %>
      <li>
        <h2>User: <%=message.user%></h2>
        <%=message.text%>
        <p>createdAt: <%=message.added%></p>
      </li>

      <%})%>
    </ul>
    <a href="/new">New Message</a>
  </body>
</html>
